apiVersion: apps/v1
kind: Deployment
metadata:
  name: pistas
  namespace: grupo1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pistas
  template:
    metadata:
      labels:
        app: pistas
    spec:
      containers:
        - name: pistas
          image: atc-pistas:latest
          imagePullPolicy: Never
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: atc-secrets
                  key: DATABASE_URL_PISTAS
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: atc-secrets
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
