apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-management
  namespace: grupo1
spec:
  selector:
    app: rabbitmq
  ports:
    - name: management
      port: 15672
      targetPort: 15672
      nodePort: 30672
  type: NodePort
