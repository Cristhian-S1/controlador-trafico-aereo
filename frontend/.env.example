# URL base del backend. En desarrollo local apunta directamente al servicio vuelos.
# En produccion (servido por Nginx) debe ser '/api' para mismo origen.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001